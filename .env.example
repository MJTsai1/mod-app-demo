# Copy to .env.local for local development. Entirely optional — this site
# has no backend and works with zero environment variables.

# Public base URL of the deployed site, used only to set the Open Graph
# metadataBase for link previews. No trailing slash.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
